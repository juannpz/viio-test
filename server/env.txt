DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=
DB_NAME=
SECRET_KEY=

'DB_HOST' puede tener como valor localhost (usando la configuracion por defecto de postgreSQL) para correr el servidor de forma local; o bien 'db' para correr el servidor en docker. 